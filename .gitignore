# Created by someone who learned the hard way what NOT to commit
# (Yes, I once committed API keys to production. Don't judge.)

# Node.js stuff
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment files (learned this lesson twice)
.env
.env.local
.env.*.local
*.key
*.pem

# Logs (these get huge fast)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.npm
.eslintcache

# Build outputs
dist/
build/
.next/
.nuxt/
.vuepress/dist/

# IDEs and editors (because everyone uses something different)
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Docker stuff
.docker/
docker-compose.override.yml

# Kubernetes secrets (don't ask why this is here)
*secret*.yaml
*secret*.yml
kubeconfig*

# Terraform state files (never, EVER commit these)
*.tfstate
*.tfstate.*
*.tfvars
.terraform/
.terraform.lock.hcl
terraform.tfplan

# Backup files (created by our backup script)
backups/
*.backup
*.bak
*.dump

# Security scan reports (contains sensitive info)
security-reports/
*.sarif

# SSL certificates (generated by our SSL script)
ssl/*.pem
ssl/*.key
ssl/*.crt
ssl/*.csr

# Test coverage
coverage/
*.lcov
.nyc_output/

# Temporary files
tmp/
temp/
.cache/

# Python (for when we add Python projects)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# Go (just in case)
*.exe
*.exe~
*.dll
*.so
*.dylib
vendor/

# Rust (why not)
target/
Cargo.lock

# Local development overrides
docker-compose.local.yml
config.local.js
.local

# macOS
.AppleDouble
.LSOverride

# Windows
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# Monitoring data (gets massive)
prometheus-data/
grafana-data/
elasticsearch-data/

# Don't commit these either (trust me on this)
.aws/
.kube/config
.ssh/
.gnupg/

# Database dumps (can be huge)
*.sql
*.db
*.sqlite

# Don't commit node_modules in any subdirectory
**/node_modules/

# Ignore any file that starts with "secret" or "private"
secret*
private*
*secret*
*private* 