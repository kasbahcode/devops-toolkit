# DevOps Toolkit Configuration
# Copy this file to .env or config/production.env and update with your actual values
# ⚠️  SECURITY WARNING: Change ALL default passwords before production use!

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SecurePass123!ChangeMe
POSTGRES_DB=devops_toolkit

# Grafana Configuration  
GRAFANA_ADMIN_PASSWORD=GrafanaSecure456!ChangeMe

# Email/SMTP Configuration
SMTP_HOST=smtp.yourdomain.com:587
SMTP_FROM=alerts@yourdomain.com
SMTP_USERNAME=alerts@yourdomain.com
SMTP_PASSWORD=SMTPSecure789!ChangeMe

# Slack Integration
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# PagerDuty Integration
PAGERDUTY_INTEGRATION_KEY=your-pagerduty-integration-key

# Registry Configuration
REGISTRY_USERNAME=your-docker-registry-username
REGISTRY_PASSWORD=your-docker-registry-password

# SSL Configuration
SSL_EMAIL=your-email@yourdomain.com
SSL_DOMAIN=yourdomain.com

# Backup Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
BACKUP_S3_BUCKET=your-backup-bucket

# Environment
ENVIRONMENT=staging 

# ⚠️  IMPORTANT: Generate strong passwords using:
# openssl rand -base64 32
# Or use a password manager like 1Password, LastPass, etc. 